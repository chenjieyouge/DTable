# 忽略系统或编辑器临时文件
.DS_Store
Thumbs.db
*.tmp
cj.txt
test.html
test.js
test.ts

# 忽略本地凭据
~/.git-credentials
.git-credentials

# 忽略环境变量
.env
.env.local
secrets.json

# node_modules
node_modules/

# 构建产物（npm publish 由 package.json files 字段控制）
dist/

.windsurf/

# 测试用例的报告不用上传, 用例需要
coverage/

# dos 也先不用, 就自己看
/docs

# 后端服务 (本地开发用的 gin + mysql, 不用提交到前端仓库)
/server/

# Go 工作区文件（本地开发配置）
go.work
go.work.sum


# 其他一些不需要的
div_table.*